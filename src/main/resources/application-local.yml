spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/internPoc
    username: dev
    password: dev1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    com.danalfintech: DEBUG
    org.hibernate.SQL: DEBUG
